let a: T extends string ? "yes" : "no";
let b: T extends A ? X : T extends B ? Y : Z;
let c: T extends A ? (T extends B ? X : Y) : Z;
let d: T extends U | V ? X : Y;
let e: T extends U[] ? X : Y;
let f: T extends A ? B extends C ? X : Y : Z;
let g: (T extends A ? X : Y)[];
let h: [A extends B ? X : Y, C];
let i: [x: A extends B ? X : Y];
let j: T extends U ? V : W;
let m: Array<T extends U ? X : Y>;
