// file header
import { x } from "mod"; // trail import

/**
 * jsdoc
 */
function /* inline name */ foo(a, b) {
  // lead-return
  return a + b; /* trail-return */
}

const empty = {}; // trail decl
// dangling-trailing
