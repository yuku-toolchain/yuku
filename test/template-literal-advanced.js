`simple`
`with ${x} substitution`
`multiple ${a} and ${b} substitutions`
`nested ${`inner ${y}`} template`
`with ${1 + 2} expression`
`start ${a} middle ${b} end`
`with\nnewline`
`with\ttab`
`with\u0041unicode`
`with\x41hex`
`${a}${b}${c}`
`multi
line
template`
