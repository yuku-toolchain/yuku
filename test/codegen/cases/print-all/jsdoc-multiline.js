/**
 * Adds two numbers.
 * @param {number} a
 * @param {number} b
 */
function add(a, b) {
    return a + b;
}

class Calculator {
    /**
     * Multiplies two numbers.
     * @param {number} a
     * @param {number} b
     */
    multiply(a, b) {
        return a * b;
    }
}
