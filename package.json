{
	"name": "yuku-root",
	"private": true,
	"scripts": {
		"dev:docs": "bun run --filter './docs' dev",
		"test:tools": "zig build test-tools",
		"npm:publish": "bumpp -r --commit 'npm %s' --push --tag --yes --release 'patch'",
		"npm:build": "napi build",
		"npm:build-release": "napi build --release && zig build gen-estree-decoder && cp ./zig-out/decode.js ./npm/yuku-parser",
		"format": "zig fmt **/*.zig",
		"test": "bun load-files && bun npm:build-release && bun test/parser/run.ts",
		"load-files": "bun profiler/load-parser-bench-files && bun test/parser/load test/parser/suite",
		"play": "bun --console-depth=20 test/playground.ts"
	},
	"devDependencies": {
		"@types/bun": "^1.3.11",
		"@types/node": "^25.5.0",
		"bumpp": "^11.0.1",
		"fast-deep-equal": "^3.1.3",
		"jest-diff": "^30.3.0",
		"napi-zig": "^0.1.11",
		"typescript": "^5.9.3",
		"yuku-parser": "workspace:*"
	},
	"workspaces": [
		"npm/**/*",
		"docs"
	]
}
