{
  "name": "yuku-root",
  "private": true,
  "scripts": {
    "build:npm": "zig build && bun run --filter './npm/**/*' build",
    "dev:docs": "bun run --filter './docs' dev",
    "test:tools": "zig build test-tools",
    "publish:npm": "bumpp -r --commit 'npm %s' --push --tag --yes --release 'patch'",
    "format": "zig fmt **/*.zig",
    "test": "bun load-files && bun build:npm && bun test/run.ts",
    "load-files": "bun profiler/load-parser-bench-files && bun test/load test/suite -b test262-pass-2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.10",
    "bumpp": "^10.4.1",
    "fast-deep-equal": "^3.1.3",
    "jest-diff": "^30.2.0",
    "typescript": "^5.9.3",
    "yuku-shared": "workspace:*"
  },
  "workspaces": [
    "npm/**/*",
    "docs"
  ]
}
